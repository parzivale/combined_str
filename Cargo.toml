[package]
name = "combined_str"
version = "0.3.0"
edition = "2024"
description = "Zero-copy, const-generic string combinator for no_std environments"
license = "MIT"

[features]
default = ["alloc"]
alloc = []
nightly = []
