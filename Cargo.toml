[package]
name = "combined_str"
version = "0.2.1"
edition = "2024"
description = "Zero-copy, const-generic string combinator for no_std environments"
license = "MIT"

[features]
default = ["alloc"]
alloc = []
